type id ( type id , type id ) {
  type id = id ( id , num ) ;
  while ( id == num ) {
    id = id + num ;
  }
  return id ;
}
type id = id + num * ( id - num ) ;
type id ( type id , type id ) {
  return id + num ;
}